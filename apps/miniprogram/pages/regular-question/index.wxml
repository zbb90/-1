<view class="page">
  <view class="card">
    <view class="form-header">常规问题</view>
    <view class="form-sub">描述现场情况，AI 帮你判断是否扣分</view>

    <view class="field">
      <view class="label">门店编码</view>
      <input
        class="input"
        placeholder="请输入门店编码"
        value="{{storeCode}}"
        bindinput="handleStoreCodeInput"
      />
    </view>

    <view class="field">
      <view class="label">问题分类</view>
      <picker
        range="{{categories}}"
        value="{{categoryIndex}}"
        bindchange="handleCategoryChange"
      >
        <view class="input picker {{categoryIndex === 0 ? 'placeholder' : ''}}">{{categories[categoryIndex]}}</view>
      </picker>
    </view>

    <view class="field">
      <view class="label">自行判断（选填）</view>
      <input
        class="input"
        placeholder="例如：扣 / 不扣 / 不确定"
        value="{{selfJudgment}}"
        bindinput="handleSelfJudgmentInput"
      />
    </view>

    <view class="field">
      <view class="label">问题描述</view>
      <textarea
        class="textarea"
        placeholder="描述现场情况，例如：分装坚果碎过期，门店反馈新制作，但新旧混放无法明确区分。"
        value="{{description}}"
        bindinput="handleDescriptionInput"
      />
    </view>

    <button class="submit" type="primary" loading="{{loading}}" bindtap="handleSubmit">
      提交问题
    </button>
  </view>
</view>
