# 数据目录

这里存放项目运行所需的结构化规则数据。

当前一期上线默认读取：

`data/templates`

这些文件已经纳入当前仓库，Vercel 线上部署也会直接读取这里。

如果后续你在本地继续整理新数据，也可以先从兄弟目录同步过来：

`../问答机器人/数据模板`

建议后续导入顺序：
1. `01_条款主表.csv`
2. `02_共识解释表.csv`
3. `03_常规问题规则表.csv`
4. `04_旧品清单表.csv`
5. `05_外购清单表.csv`
